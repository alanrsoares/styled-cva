{
  "name": "styled-cva",
  "version": "0.5.0",
  "private": true,
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "bun run --filter @styled-cva/eslint-plugin build && bun run --filter @styled-cva/react build && bun run --filter @styled-cva/docs build",
    "dev": "bun run --filter @styled-cva/docs dev",
    "test": "bun run --filter @styled-cva/* test",
    "lint": "bun run --filter @styled-cva/* lint",
    "clean": "bun run --filter @styled-cva/eslint-plugin clean && bun run --filter @styled-cva/react clean",
    "format": "bun run --filter @styled-cva/* format",
    "release:react": "bun run build && npm publish -w @styled-cva/react",
    "release:eslint-plugin": "bun run build && npm publish -w @styled-cva/eslint-plugin"
  },
  "engines": {
    "node": ">=20"
  }
}
