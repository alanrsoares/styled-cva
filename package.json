{
  "name": "styled-cva",
  "version": "0.5.1",
  "private": true,
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "bun run --filter @styled-cva/core build && bun run --filter @styled-cva/eslint-plugin build && bun run --filter @styled-cva/react build && bun run --filter @styled-cva/solid build && bun run --filter @styled-cva/vue build && bun run --filter @styled-cva/docs build",
    "dev": "bun run --filter @styled-cva/docs dev",
    "test": "bun run --filter @styled-cva/* test",
    "lint": "bun run --filter @styled-cva/* lint",
    "clean": "bun run --filter @styled-cva/core clean && bun run --filter @styled-cva/eslint-plugin clean && bun run --filter @styled-cva/react clean && bun run --filter @styled-cva/solid clean && bun run --filter @styled-cva/vue clean",
    "format": "bun run --filter @styled-cva/* format",
    "release:react": "bun run build && npm publish -w @styled-cva/react",
    "release:solid": "bun run build && npm publish -w @styled-cva/solid",
    "release:vue": "bun run build && npm publish -w @styled-cva/vue",
    "release:eslint-plugin": "bun run build && npm publish -w @styled-cva/eslint-plugin",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@ianvs/prettier-plugin-sort-imports": "^4.7.0",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2"
  }
}
