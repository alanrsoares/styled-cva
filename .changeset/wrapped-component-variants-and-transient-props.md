---
"@styled-cva/react": patch
"@styled-cva/solid": patch
"@styled-cva/vue": patch
---

Strip transient `$`-prefixed props when wrapping custom components with `tw(Component)` and enable variant declaration via the `(base, config)` shorthand and `.cva()` method.
