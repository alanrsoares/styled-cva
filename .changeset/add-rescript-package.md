---
"@styled-cva/rescript": minor
---

Add `@styled-cva/rescript` package providing zero-cost ReScript v12 bindings for `@styled-cva/react` with build-time codegen for 72+ HTML element factories (`Tw.div`, `Tw.button`, etc.), `cva()`, and `cn()`.
